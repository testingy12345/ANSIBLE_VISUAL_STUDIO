---
- hosts: all
  become: yes
  tasks:
  

  - name: I want to install apache2  webserver on  ubuntu  
    apt:
      name:  apache2
      state: present

  - name: I want to start my apache2 server
    service:
       name: apache2
       state: started

  